---

{{ if .Vars.amazon2cis_level_1 }}
  {{ if .Vars.amazon2cis_rule_7_1_10 }}
file:
  /etc/security/opasswd_perms:
    title: 7.1.10 | Ensure access to /etc/security/opasswd is configured
    exists: true
    path: /etc/security/opasswd
    mode:
      or:
      - '0644'
      - '0640'
      - '0600'
      - '0400'
    owner: root
    group: root
    meta:
      server: 1
      workstation: NA
      CIS_ID:
      - 7.1.10
      NIST800-53R5:
      - AC-3
      - MP-2
  {{ end }}
{{ end }}
