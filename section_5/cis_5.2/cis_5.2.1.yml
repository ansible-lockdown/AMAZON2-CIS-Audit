---

{{ if .Vars.amazon2cis_level_1 }}
  {{ if .Vars.amazon2cis_rule_4_2_1 }}
file:
  sshd_config_perms:
    title: 4.2.1 | Ensure permissions on /etc/ssh/sshd_config are configured
    path: /etc/ssh/sshd_config
    exists: true
    mode: '0600'
    owner: root
    group: root
    meta:
      server: 1
      workstation: NA
      CIS_ID:
      - 4.2.1
      NIST800-53R5:
      - AC-3
      - MP-2
  {{ end }}
{{ end }}
