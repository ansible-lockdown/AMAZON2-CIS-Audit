---

{{ if .Vars.amazon2cis_config_aide }}
  {{ if .Vars.amazon2cis_level_1 }}
  {{ if .Vars.amazon2cis_rule_6_3_2 }}
    {{ if eq .Vars.amazon2cis_aide_scan "cron" }}
command:
  aide_cron:
    title: 6.3.2 | Ensure filesystem integrity is regularly checked
    exit-status:
      or:
      - 0
      - 2
    exec: "grep -rs aide /etc/cron.* /etc/crontab /var/spool/cron/*"
    stdout:
    - '!/^#/'
    {{ end }}
    meta:
      server: 1
      workstation: NA
      CIS_ID:
      - 6.3.2
      NIST800-53R5: AU-2
# Can be enabled if using timer and service files
service:
    {{ if eq .Vars.amazon2cis_aide_scan "timer" }}
  aidecheck:
    title: 6.3.2 | Ensure filesystem integrity is regularly checked
    enabled: true
    running: true
    skip: false
    meta:
      server: 1
      workstation: NA
      CIS_ID:
      - 6.3.2
      NIST800-53R5: AU-2
  aidecheck.timer:
    title: 6.3.2 | Ensure filesystem integrity is regularly checked
    enabled: true
    running: true
    skip: false
    meta:
      server: 1
      workstation: NA
      CIS_ID:
      - 6.3.2
      NIST800-53R5: AU-2
    {{ end }}
  {{ end }}
  {{ end }}
{{ end }}
