---

{{ if .Vars.amazon2cis_level_2 }}
  {{ if .Vars.amazon2cis_rule_6_2_4_3 }}
command:
  audit_logfile_owner:
    title: 6.2.4.3 | Ensure audit log files owner is configured
    exec: for file in `grep ^log_file /etc/audit/auditd.conf | awk '{ print $NF }'`; do stat -Lc "%n_%U" $file; done
    exit-status: 0
    stdout:
    - '/.*_root$/'
    meta:
      server: 2
      workstation: NA
      CIS_ID:
      - 6.2.4.3
      NIST800-53R5: NA
  {{ end }}
{{ end }}
