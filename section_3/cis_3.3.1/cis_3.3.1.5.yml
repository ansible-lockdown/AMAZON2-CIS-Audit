---

{{ if not .Vars.amazon2cis_is_router }}
  {{ if .Vars.amazon2cis_level_1 }}
    {{ if .Vars.amazon2cis_rule_3_3_1_5 }}
kernel-param:
  net.ipv4.conf.default.send_redirects:
    title: 3.3.1.5 | Ensure net.ipv4.conf.default.send_redirects is configured
    value: '0'
    meta:
      server: 1
      workstation: NA
      CIS_ID:
      - 3.3.1.5
      NIST800-53R5:
      - CM-6
      - CM-7
    {{ end }}
  {{ end }}
{{ end }}
