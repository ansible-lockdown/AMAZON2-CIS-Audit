---

{{ if .Vars.amazon2cis_level_1 }}
package:
  {{ if not .Vars.amazon2cis_ftp_client }}
    {{ if .Vars.amazon2cis_rule_2_3_1 }}
  ftp_client:
    title: 2.3.1 | Ensure ftp client is not installed
    name: ftp
    installed: false
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 2.3.1
      NIST800-53R5:
      - CM-7
    {{ end }}
  {{ end }}
{{ end }}
