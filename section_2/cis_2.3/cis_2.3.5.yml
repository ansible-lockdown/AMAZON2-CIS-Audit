---

{{ if .Vars.amazon2cis_level_1 }}
package:
  {{ if not .Vars.amazon2cis_tftp_client }}
    {{ if .Vars.amazon2cis_rule_2_3_5}}
  tftp_client:
    title: 2.3.5 | Ensure tftp client is not installed
    name: tftp
    installed: false
    meta:
      server: 1
      workstation: NA
      CIS_ID:
      - 2.3.5
      NIST800-53R5:
      - CM-7
    {{ end }}
  {{ end }}
{{ end }}
