---

{{ if .Vars.amazon2cis_level_1 }}
package:
  {{ if not .Vars.amazon2cis_ypbind_required }}
    {{ if .Vars.amazon2cis_rule_2_3_3 }}
  nis_client:
    title: 2.3.3 | Ensure nis client is not installed
    name: ypbind
    installed: false
    meta:
      server: 1
      workstation: NA
      CIS_ID:
      - 2.3.3
      NIST800-53R5:
      - CM-7
    {{ end }}
  {{ end }}
{{ end }}
