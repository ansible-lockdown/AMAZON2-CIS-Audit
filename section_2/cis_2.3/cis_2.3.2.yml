---

{{ if .Vars.amazon2cis_level_1 }}
package:
  {{ if not .Vars.amazon2cis_openldap_clients_required}}
    {{ if .Vars.amazon2cis_rule_2_3_2 }}
  ldap_client:
    title: 2.3.2 | Ensure ldap client is not installed
    name: openldap-clients
    installed: false
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 2.3.2
      NIST800-53R5:
      - CM-7
    {{ end }}
  {{ end }}
{{ end }}
