---

{{ if .Vars.amazon2cis_level_1 }}
package:
  {{ if not .Vars.amazon2cis_telnet_required }}
    {{ if .Vars.amazon2cis_rule_2_3_4 }}
  telnet_client:
    title: 2.3.4 | Ensure telnet client is not installed
    name: telnet
    installed: false
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 2.3.4
      NIST800-53R5:
      - CM-7
    {{ end }}
  {{ end }}
{{ end }}
