---

{{ if .Vars.amazon2cis_level_1 }}
  {{ if .Vars.amazon2cis_rule_2_2_5 }}
    {{ if not .Vars.amazon2cis_dnsmasq_server }}
package:
  dnsmasq_pkg:
    title: 2.2.5 | Ensure dnsmasq services are not in use | pkg removed
    name: dnsmasq
    installed: false
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 2.2.5
      NIST800-53R5:
      - CM-7
      {{ end }}
      {{ if .Vars.amazon2cis_dnsmasq_mask }}
file:
  dnsmasq_service_masked:
    title: 2.2.5 | Ensure dnsmasq services are not in use | masked
    path: /etc/systemd/system/dnsmasq.service
    exists: true
    filetype: symlink
    linked-to: /dev/null
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 2.2.5
      NIST800-53R5:
      - CM-7
    {{ end }}
  {{ end }}
{{ end }}
