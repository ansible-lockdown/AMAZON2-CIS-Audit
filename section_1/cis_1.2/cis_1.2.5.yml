---

{{ if .Vars.amazon2cis_run_heavy_tests }}
  {{ if .Vars.amazon2cis_level_1 }}
    {{ if .Vars.amazon2cis_rule_1_2_5 }}
command:
  security-updates:
    title: 1.2.5 | Ensure updates, patches, and additional security software are installed
    exit-status:
      or:
      - 0
      - 1
    timeout: {{ .Vars.timeout_ms }}
    exec: yum check-update | grep -c amzn2-core
    stdout:
    - '0'
    - '!/^[1-9]/'
    meta:
      server: 1
      workstation: NA
      CIS_ID:
      - 1.2.5
      NIST800-53R5:
      - SI-2
    {{ end }}
  {{ end }}
{{ end }}
