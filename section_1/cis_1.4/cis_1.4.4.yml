---

{{ if .Vars.amazon2cis_level_1 }}
  {{ if .Vars.amazon2cis_rule_1_4_4 }}
file:
  core_dumps_storage:
    title: 1.4.4 | Ensure core dumps are restricted | security/limits.conf
    path: /etc/systemd/coredump.conf
    exists: true
    contents:
    - '/^Storage\s*=\s*none/'
    meta:
      server: 1
      workstation: NA
      CIS_ID:
      - 1.4.4
      NIST800-53R5:
  {{ end }}
{{ end }}
